# Optional local-only overrides for scripts/start-stack.ps1.
# Copy to .env.local and set values for the MySQL server running on this PC.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=909014851
DB_NAME=shoes_store_db

# Usually leave this blank. start-stack.ps1 fills it from the current ngrok URL.
TELEGRAM_WEBAPP_URL=
